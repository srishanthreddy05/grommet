'use client';

export default function AnnouncementBar() {
  return (
    <div className="sticky top-0 z-50 bg-slate-900 text-white py-2 text-center text-sm">
      <p>
        🚚 Free shipping on orders over ₹500 | Fast delivery across India
      </p>
    </div>
  );
}
